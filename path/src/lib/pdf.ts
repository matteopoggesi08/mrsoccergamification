import jsPDF from'jspdf';export function makePdf(title:string,sections:{title:string,lines:string[]}[],file:string){const d=new jsPDF();let y=18;d.setFontSize(20);d.text(title,15,y);y+=12;d.setFontSize(10);for(const s of sections){if(y>270){d.addPage();y=18}d.setFontSize(13);d.text(s.title,15,y);y+=7;d.setFontSize(9);for(const l of s.lines){if(y>285){d.addPage();y=18}d.text(l.slice(0,115),15,y);y+=5}y+=4}d.save(file)}