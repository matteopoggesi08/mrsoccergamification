import{useEffect,useState}from'react';import{ChevronRight,Plus,Users,Clock3,ShieldCheck,LogOut}from'lucide-react';import{useNavigate}from'react-router-dom';import{supabase}from'../lib/supabase';import{seasonCode}from'../lib/utils';import{Modal}from'../components/ui';import{Layout}from'../components/Layout';import{useToast}from'../components/Toast';import{logout}from'../lib/auth';import{Season}from'../types';
export default function Dashboard(){const[seasons,setSeasons]=useState<Season[]>([]),[invited,setInvited]=useState<any[]>([]),[requests,setRequests]=useState<any[]>([]),[modal,setModal]=useState<'create'|'join'|null>(null),[f,setF]=useState({name:'',team_name:'',sporting_year:'2026/2027'}),[code,setCode]=useState(''),nav=useNavigate(),toast=useToast();async function load(){const{data:{user}}=await supabase.auth.getUser();if(!user)return;const[a,b,c]=await Promise.all([supabase.from('seasons').select('*').eq('owner_id',user.id).order('created_at',{ascending:false}),supabase.from('season_members').select('*,seasons(*)').eq('user_id',user.id).eq('status','ACTIVE').neq('role','OWNER'),supabase.from('season_access_requests').select('*,seasons(name,team_name)').eq('requester_id',user.id).eq('status','PENDING')]);setSeasons(a.data||[]);setInvited(b.data||[]);setRequests(c.data||[])}useEffect(()=>{load()},[]);async function create(e:React.FormEvent){e.preventDefault();const{data:{user}}=await supabase.auth.getUser();if(!user)return;const{data,error}=await supabase.from('seasons').insert({...f,owner_id:user.id,access_code:seasonCode()}).select().single();if(error){toast.push(error.message,'error');return}const{error:me}=await supabase.from('season_members').upsert({season_id:data.id,user_id:user.id,role:'OWNER',status:'ACTIVE',accepted_at:new Date().toISOString(),permissions:{players:true,sessions:true,attendance:true,workload:true,games:true,matches:true,leaderboard:true,fines:true,reports:true,members:true}},{onConflict:'season_id,user_id'});if(me){toast.push(me.message,'error');return}toast.push('Stagione creata');setModal(null);nav(`/season/${data.id}`)}async function join(e:React.FormEvent){e.preventDefault();const{data:{user}}=await supabase.auth.getUser();if(!user)return;const{data:s}=await supabase.from('seasons').select('id,name').eq('access_code',code.trim().toUpperCase()).maybeSingle();if(!s){toast.push('Codice non valido.','error');return}const{error}=await supabase.from('season_access_requests').upsert({season_id:s.id,requester_id:user.id,status:'PENDING'},{onConflict:'season_id,requester_id'});if(error)toast.push(error.message,'error');else{toast.push('Richiesta inviata');setModal(null);setCode('');load()}}return <Layout><div className="page"><div className="head"><div><small>DASHBOARD</small><h1>Le tue stagioni</h1><p>Apri una stagione o creane una nuova.</p></div><button className="secondary" onClick={logout}><LogOut size={16}/>Esci</button></div><div className="hero-actions"><button className="primary" onClick={()=>setModal('create')}><Plus/>Crea stagione</button><button className="secondary" onClick={()=>setModal('join')}>Accedi con codice</button></div><section><h2 className="section-title">Le mie stagioni</h2>{seasons.length?<div className="cards">{seasons.map(s=><button className="season-card" key={s.id} onClick={()=>nav(`/season/${s.id}`)}><div><small>{s.sporting_year}</small><h3>{s.name}</h3><p>{s.team_name}</p><code>{s.access_code}</code></div><ChevronRight/></button>)}</div>:<div className="panel"><p>Nessuna stagione creata.</p></div>}</section>{invited.length>0&&<section><h2 className="section-title">Stagioni condivise</h2><div className="cards">{invited.map(m=><button className="season-card" key={m.id} onClick={()=>nav(`/season/${m.season_id}`)}><div><small>{m.role}</small><h3>{m.seasons?.name}</h3><p>{m.seasons?.team_name}</p></div><ChevronRight/></button>)}</div></section>}{requests.length>0&&<section><h2 className="section-title">Richieste in attesa</h2><div className="cards">{requests.map(r=><div className="info" key={r.id}><div><b>{r.seasons?.name||'Stagione'}</b><small>Richiesta inviata · in attesa dell’OWNER</small></div><Clock3/></div>)}</div></section>}{modal==='create'&&<Modal title="Nuova stagione" close={()=>setModal(null)}><form className="form" onSubmit={create}><label>Nome stagione<input autoFocus required value={f.name} onChange={e=>setF({...f,name:e.target.value})}/></label><label>Nome squadra<input required value={f.team_name} onChange={e=>setF({...f,team_name:e.target.value})}/></label><label>Stagione sportiva<input required value={f.sporting_year} onChange={e=>setF({...f,sporting_year:e.target.value})}/></label><button className="primary wide">Crea stagione</button></form></Modal>}{modal==='join'&&<Modal title="Richiedi accesso" close={()=>setModal(null)}><form className="form" onSubmit={join}><label>Codice privato<input autoFocus maxLength={11} value={code} onChange={e=>setCode(e.target.value.toUpperCase())}/></label><button className="primary wide">Invia richiesta</button></form></Modal>}</div></Layout>}
