import{useEffect,useState}from'react';import{History}from'lucide-react';import{useParams}from'react-router-dom';import{supabase}from'../lib/supabase';import{Page}from'../components/Layout';import{useSeason}from'../hooks/useSeason';import{Empty}from'../components/ui';
export default function Logs(){const{seasonId}=useParams(),{season,loading}=useSeason(seasonId),[rows,setRows]=useState<any[]>([]);useEffect(()=>{supabase.from('activity_logs').select('*').eq('season_id',seasonId).order('created_at',{ascending:false}).limit(300).then(({data})=>setRows(data||[]))},[seasonId]);if(loading||!season)return <div className="center"><div className="spinner"/></div>;return <Page season={season} title="Log attività" sub="Storico delle modifiche registrate dal database">{rows.length?<div className="cards">{rows.map(x=><div className="info" key={x.id}><div><b>{x.action} · {x.entity_type}</b><small>{new Date(x.created_at).toLocaleString('it-IT')} · {x.actor_user_id||'sistema'}</small></div><History size={17}/></div>)}</div>:<Empty title="Nessun log" text="Le operazioni importanti vengono registrate automaticamente."/>}</Page>}
