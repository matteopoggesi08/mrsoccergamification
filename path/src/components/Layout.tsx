import{useEffect,useState}from'react';import{useLocation,useNavigate}from'react-router-dom';import{Activity,ArrowLeft,BarChart3,CalendarDays,Check,ChevronRight,CircleDollarSign,ClipboardList,FileText,Home,Menu,ShieldCheck,Trophy,Users}from'lucide-react';import{Season}from'../types';import{logout}from'../lib/auth';
const items=(sid:string)=>[[`/season/${sid}`,'Home',Home],[`/season/${sid}/players`,'Rosa',Users],[`/season/${sid}/sessions`,'Sedute',CalendarDays],[`/season/${sid}/attendance`,'Presenze',ClipboardList],[`/season/${sid}/workload`,'Carichi',Activity],[`/season/${sid}/leaderboard`,'Classifica',Trophy],[`/season/${sid}/fines`,'Multe',CircleDollarSign],[`/season/${sid}/reports`,'Report',FileText],[`/season/${sid}/members`,'Staff',ShieldCheck],[`/season/${sid}/log`,'Log',BarChart3]] as const;
export function Layout({children,season}:{children:React.ReactNode;season?:Season|null}){const nav=useNavigate(),loc=useLocation(),[menu,setMenu]=useState(false);useEffect(()=>setMenu(false),[loc.pathname]);const list=season?items(season.id):[];return <div className="app"><header className="top"><button className="brand" onClick={()=>nav(season?`/season/${season.id}`:'/dashboard')}><i>S</i><span>SOCCERMRGAMIFICATION</span></button>{season&&<span className="season-chip">{season.team_name} · {season.sporting_year}</span>}<div className="top-actions">{season&&<button className="top-code" onClick={()=>navigator.clipboard?.writeText(season.access_code)} title="Copia codice">{season.access_code}</button>}<button className="icon mobile" onClick={()=>setMenu(x=>!x)}><Menu/></button></div></header><div className="layout">{season&&<aside className={menu?'side open':'side'}>{list.map(([u,l,I])=><button key={u} className={loc.pathname===u||loc.pathname.startsWith(u+'/')?'nav active':'nav'} onClick={()=>nav(u)}><I size={17}/>{l}</button>)}<div className="grow"/><button className="nav" onClick={()=>nav('/dashboard')}><ArrowLeft size={17}/>Dashboard</button><button className="nav danger-nav" onClick={()=>logout()}><Check size={17}/>Esci</button></aside>}<main>{children}</main></div>{season&&<nav className="bottom">{list.slice(0,5).map(([u,l,I])=><button key={u} className={loc.pathname===u||loc.pathname.startsWith(u+'/')?'active':''} onClick={()=>nav(u)}><I size={18}/><span>{l}</span></button>)}</nav>}</div>}
export function Page({title,sub,season,actions,children}:{title:string;sub?:string;season:Season|null;actions?:React.ReactNode;children:React.ReactNode}){return <Layout season={season}><div className="page"><div className="head"><div><small>STAGIONE</small><h1>{title}</h1>{sub&&<p>{sub}</p>}</div>{actions&&<div className="head-actions">{actions}</div>}</div>{children}</div></Layout>}
