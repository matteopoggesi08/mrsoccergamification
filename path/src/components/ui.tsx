import{X,FileText,LoaderCircle}from'lucide-react';
export function Center({text='Caricamento…'}:{text?:string}){return <div className="center"><LoaderCircle className="spin"/><span>{text}</span></div>}
export function Modal({title,close,children,wide=false}:{title:string;close:()=>void;children:React.ReactNode;wide?:boolean}){return <div className="backdrop" onMouseDown={e=>{if(e.target===e.currentTarget)close()}}><div className={wide?'modal wide-modal':'modal'}><div className="modal-head"><h2>{title}</h2><button className="icon" onClick={close}><X/></button></div>{children}</div></div>}
export function Confirm({title,text,confirmLabel='Conferma',danger=false,onClose,onConfirm,busy=false}:{title:string;text:string;confirmLabel?:string;danger?:boolean;onClose:()=>void;onConfirm:()=>void;busy?:boolean}){return <Modal title={title} close={onClose}><p className="confirm-text">{text}</p><div className="modal-actions"><button className="secondary" onClick={onClose} disabled={busy}>Annulla</button><button className={danger?'danger':'primary'} onClick={onConfirm} disabled={busy}>{busy?'Attendo…':confirmLabel}</button></div></Modal>}
export function Empty({title,text,action,click}:{title:string;text:string;action?:string;click?:()=>void}){return <div className="empty"><div className="empty-icon"><FileText/></div><h3>{title}</h3><p>{text}</p>{action&&<button className="secondary" onClick={click}>{action}</button>}</div>}
export function Stat({label,value,icon}:{label:string;value:React.ReactNode;icon:React.ReactNode}){return <div className="stat"><span>{icon}</span><small>{label}</small><b>{value}</b></div>}
export function SpinnerButton({children,busy=false,className='primary'}:{children:React.ReactNode;busy?:boolean;className?:string}){return <button className={className} disabled={busy}>{busy?<LoaderCircle className="spin" size={17}/>:null}{children}</button>}
export function FieldHint({children}:{children:React.ReactNode}){return <small className="field-hint">{children}</small>}
